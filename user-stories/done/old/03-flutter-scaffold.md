# Issue 3: Flutter project scaffold: Windows target + Material 3 dark/deepPurple theme

## What to build

Create the Flutter Windows desktop project with the correct theme applied globally. No functionality — just a runnable app window with the right look established from the start so all subsequent UI slices inherit it.

## Acceptance criteria

- [ ] Flutter Windows project compiles and launches as a native desktop window
- [ ] `ThemeData` uses Material 3 with `ColorScheme.fromSeed(seedColor: Colors.deepPurple)` in dark mode
- [ ] App window title is set to the product name
- [ ] No placeholder widgets or debug banners in release mode

## Blocked by

None — can start immediately.
