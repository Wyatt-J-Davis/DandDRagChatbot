# Issue 34: `flutter_quill` rich-text editor widget (bold, italic, headers, lists)

## What to build

Add a `QuillEditor` and `QuillToolbar` to the Note Editor page with formatting support for bold, italic, headers, and lists. No backend wiring yet — just the editor widget rendering correctly.

## Acceptance criteria

- [ ] `QuillEditor` and `QuillToolbar` are visible on the Note Editor page
- [ ] Toolbar includes bold, italic, header level, and list (ordered/unordered) controls
- [ ] Formatting is applied correctly when toolbar buttons are used
- [ ] Editor is scrollable and fills the available content area

## Blocked by

- Issue 6: Navigation rail (Note Editor page stub exists)
